import { Typography } from '@mui/material'
import styles from './AdminLogin.module.css'

const AdminLogin = () => {
  return (
    <div className={styles.container}>
      <Typography variant="h3" color='info' className={styles.title}>
        JOIN US NOW!!
      </Typography>

    </div>
  )
}

export default AdminLogin